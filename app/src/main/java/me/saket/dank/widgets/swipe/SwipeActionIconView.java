package me.saket.dank.widgets.swipe;

import android.content.Context;
import androidx.appcompat.widget.AppCompatImageView;
import android.util.AttributeSet;

/**
 * Note: prefer using equi-sized icons for all gestures. Different sizes aren't supported very well.
 */
public class SwipeActionIconView extends AppCompatImageView {

  public SwipeActionIconView(Context context, AttributeSet attrs) {
    super(context, attrs);
  }
}
